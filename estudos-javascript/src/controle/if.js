// O if é uma estrutura condicional que permite executar um bloco de código apenas se uma condição for verdadeira.


if(condicional){
    // Código executado se a condição for verdadeira
} else {
    // Código executado se a condição for falsa
}